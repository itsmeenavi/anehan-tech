import { Typography, Container } from '@mui/material';

const ContactUs = () => (
  <Container>
    <Typography variant="h4" component="h1" gutterBottom>
      Contact Us
    </Typography>
    <Typography variant="body1">
      Get in touch with Anehan.
    </Typography>
  </Container>
);

export default ContactUs; 